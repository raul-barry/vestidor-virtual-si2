export interface AdminVariant { id_variante: number; sku: string; producto: string; talla: string; color: string; estado: string; }
export interface VariantRequest { sku: string; id_talla: number; id_color: number; }
