import { provideHttpClient } from '@angular/common/http';
import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing';
import { TestBed } from '@angular/core/testing';
import { InventoryAdminService } from './inventory-admin.service';
describe('InventoryAdminService',()=>{let service:InventoryAdminService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(InventoryAdminService);http=TestBed.inject(HttpTestingController)});afterEach(()=>http.verify());it('creates and changes stock',()=>{service.createInventory({id_variante:1,id_sucursal:2,stock:5}).subscribe();let r=http.expectOne('http://localhost:8000/api/admin/inventory');expect(r.request.method).toBe('POST');r.flush({});service.increaseStock(1,{cantidad:2,motivo:'Compra'}).subscribe();r=http.expectOne('http://localhost:8000/api/admin/inventory/1/increase');expect(r.request.method).toBe('POST');r.flush({});service.decreaseStock(1,{cantidad:1,motivo:'Salida'}).subscribe();r=http.expectOne('http://localhost:8000/api/admin/inventory/1/decrease');r.flush({});service.adjustStock(1,{nuevo_stock:3,motivo:'Ajuste'}).subscribe();r=http.expectOne('http://localhost:8000/api/admin/inventory/1/adjust');expect(r.request.method).toBe('PUT');r.flush({})});it('gets movements',()=>{service.getMovements(1).subscribe();const r=http.expectOne('http://localhost:8000/api/admin/inventory/1/movements');expect(r.request.method).toBe('GET');r.flush([])})});
