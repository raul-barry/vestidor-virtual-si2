import { Component, OnInit, inject } from '@angular/core';
import { MatButtonModule } from '@angular/material/button';
import { MatTableModule } from '@angular/material/table';
import { AdminUser } from '../../models/user-admin.model';
import { UserAdminService } from '../../services/user-admin.service';
@Component({selector:'app-user-management',standalone:true,imports:[MatButtonModule,MatTableModule],template:`<section class="page"><h1>Usuarios</h1><div class="table-scroll" role="region" aria-label="Tabla de datos" tabindex="0"><table mat-table [dataSource]="items"><ng-container matColumnDef="nombre"><th mat-header-cell *matHeaderCellDef>Nombre</th><td mat-cell *matCellDef="let u">{{u.nombres}} {{u.apellidos}}</td></ng-container><ng-container matColumnDef="correo"><th mat-header-cell *matHeaderCellDef>Correo</th><td mat-cell *matCellDef="let u">{{u.correo}}</td></ng-container><ng-container matColumnDef="rol"><th mat-header-cell *matHeaderCellDef>Rol</th><td mat-cell *matCellDef="let u">{{u.rol}}</td></ng-container><ng-container matColumnDef="estado"><th mat-header-cell *matHeaderCellDef>Estado</th><td mat-cell *matCellDef="let u">{{u.estado}}</td></ng-container><ng-container matColumnDef="acciones"><th mat-header-cell *matHeaderCellDef>Acciones</th><td mat-cell *matCellDef="let u"><button mat-button (click)="toggle(u)">{{u.estado==='ACTIVO'?'Desactivar':'Activar'}}</button></td></ng-container><tr mat-header-row *matHeaderRowDef="cols"></tr><tr mat-row *matRowDef="let row;columns:cols"></tr></table></div></section>`,styles:['.page{padding:2rem}table{width:100%}']})
export class UserManagementComponent implements OnInit { private service=inject(UserAdminService);items:AdminUser[]=[];cols=['nombre','correo','rol','estado','acciones'];ngOnInit(){this.load()}load(){this.service.list().subscribe(i=>this.items=i)}toggle(u:AdminUser){this.service.changeStatus(u.id_usuario,u.estado==='ACTIVO'?'INACTIVO':'ACTIVO').subscribe(()=>this.load())} }
