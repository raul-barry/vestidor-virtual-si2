import { Component, OnInit, inject } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { ApiService } from '../../../../core/services/api.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<main class="page"><h1>Historial de ventas</h1><form (ngSubmit)="load()"><label>Desde<input type="date" [(ngModel)]="f.fecha_desde" name="from"></label><label>Hasta<input type="date" [(ngModel)]="f.fecha_hasta" name="to"></label><label>Cliente<select [(ngModel)]="f.id_cliente" name="client"><option [ngValue]="0">Todos</option>@for(x of opt.clientes;track x.id){<option [ngValue]="x.id">{{x.nombre}}</option>}</select></label><label>Vendedor<select [(ngModel)]="f.id_vendedor" name="seller"><option [ngValue]="0">Todos</option>@for(x of opt.vendedores;track x.id){<option [ngValue]="x.id">{{x.nombre}}</option>}</select></label><label>Sucursal<select [(ngModel)]="f.id_sucursal" name="branch"><option [ngValue]="0">Todas</option>@for(x of opt.sucursales;track x.id){<option [ngValue]="x.id">{{x.nombre}}</option>}</select></label><label>Pago<select [(ngModel)]="f.metodo_pago" name="pay"><option value="">Todos</option><option>EFECTIVO</option><option>QR</option><option>TARJETA</option></select></label><label>Estado<input [(ngModel)]="f.estado" name="state"></label><label>Tipo<select [(ngModel)]="f.tipo_venta" name="type"><option value="">Todos</option><option>ONLINE</option><option>PRESENCIAL</option></select></label><button>Filtrar</button></form><div class="table-scroll"><table><tr><th>Fecha/hora</th><th>Venta/Pedido</th><th>Cliente</th><th>Tipo cliente</th><th>Vendedor/Cajero</th><th>Sucursal</th><th>Método pago</th><th>Total</th><th>Estado</th><th>Tipo venta</th><th>Acciones</th></tr>@for(r of rows;track r.id_pedido){<tr><td>{{r.fecha|date:'dd/MM/yyyy HH:mm'}}</td><td>#{{r.id_pedido}}</td><td>{{r.cliente}}</td><td>{{r.tipo_cliente}}</td><td>{{r.vendedor}}</td><td>{{r.sucursal}}</td><td>{{r.metodo_pago}}</td><td>{{r.total}}</td><td>{{r.estado}}</td><td>{{r.tipo_venta}}</td><td><button (click)="detail(r.id_pedido)">Ver detalle</button></td></tr>}</table></div>@if(selected){<section><h2>Venta #{{selected.id_pedido}}</h2>@for(d of selected.detalles;track d.producto){<p>{{d.producto}} · {{d.talla}} · {{d.color}} · {{d.cantidad}} × {{d.precio_unitario}} = {{d.subtotal}}</p>}<p>Total: {{selected.total}} · Pago: {{selected.pago?.metodo_pago || 'Pendiente'}}</p><p>Entrega: {{selected.tipo_entrega}} {{selected.entrega?.direccion || selected.entrega?.sucursal || ''}}</p></section>}</main>`,styles:[`.page{padding:2rem}form{display:flex;flex-wrap:wrap;gap:.75rem}label{display:grid}table{width:100%;border-collapse:collapse;font-size:.85rem}th,td{padding:.5rem;border-bottom:1px solid #ddd;text-align:left}section{margin-top:1rem;padding:1rem;border:1px solid #ddd}`]}) export class SalesHistoryComponent implements OnInit{private api=inject(ApiService);rows:any[]=[];selected:any;opt:any={clientes:[],vendedores:[],sucursales:[]};f:any={fecha_desde:'',fecha_hasta:'',id_cliente:0,id_vendedor:0,id_sucursal:0,metodo_pago:'',estado:'',tipo_venta:''};ngOnInit(){this.api.get<any>('/api/commerce/sales-history/options').subscribe(x=>this.opt=x);this.load()}load(){const q=new URLSearchParams();Object.entries(this.f).forEach(([k,v])=>{if(v)q.set(k,String(v))});this.api.get<any[]>('/api/commerce/sales-history?'+q).subscribe(x=>this.rows=x)}detail(id:number){this.api.get<any>('/api/commerce/sales-history/'+id).subscribe(x=>this.selected=x)}}
